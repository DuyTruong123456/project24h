/**
 * @format
 */

import { AppRegistry } from "react-native";
import ReworkApp from "./ReworkApp";
import { name as appName } from "./app.json";

AppRegistry.registerComponent(appName, () => ReworkApp);
